#!/bin/bash

# Fetch the latest changes from the remote repository
git fetch origin

# Switch to the deidine branch
git checkout deidine

# Merge changes from the main branch into the deidine branch
git merge origin/main

# Push the updated deidine branch to the remote repository
git push origin deidine

echo "Synchronization from main to deidine completed."
